{
  "name": "estoque-erp",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose stop postgres",
    "db:logs": "docker compose logs -f postgres",
    "migrate": "npm run prisma:migrate:deploy --workspace backend",
    "dev:user": "npm run dev:user --workspace backend",
    "dev:setup": "npm run db:up && npm run migrate && npm run dev:user",
    "dev": "npm run dev:setup && concurrently -n backend,frontend -c green,cyan \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:docker": "docker compose -f docker-compose.dev.yml up --build",
    "dev:docker:down": "docker compose -f docker-compose.dev.yml down",
    "dev:docker:reset": "docker compose -f docker-compose.dev.yml down -v",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "start:frontend": "npm run start --workspace frontend",
    "start:prod": "docker compose -f docker-compose.prod.yml up --build",
    "prod:up": "docker compose -f docker-compose.prod.yml up --build -d",
    "prod:down": "docker compose -f docker-compose.prod.yml down",
    "prod:logs": "docker compose -f docker-compose.prod.yml logs -f",
    "prod:reset": "docker compose -f docker-compose.prod.yml down -v",
    "repo:commit": "powershell -ExecutionPolicy Bypass -File .\\scripts\\git-commit.ps1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "drizzle-kit": "^0.31.10"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1"
  }
}
