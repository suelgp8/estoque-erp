# Preset de producao.

NODE_ENV=production
HOST=0.0.0.0
PORT=3000

DB_HOST=postgres
DB_PORT=5432
DB_NAME=estoque_erp_prod
DB_USER=estoque_prod
DB_PASSWORD=troque_esta_senha
DB_SCHEMA=public
# DATABASE_URL=postgresql://estoque_prod:troque_esta_senha@postgres:5432/estoque_erp_prod?schema=public

POSTGRES_DB=estoque_erp_prod
POSTGRES_USER=estoque_prod
POSTGRES_PASSWORD=troque_esta_senha

PRISMA_BOOTSTRAP_MODE=migrate

JWT_SECRET=troque_por_um_secret_grande_e_aleatorio
JWT_EXPIRES_IN=8h
BCRYPT_SALT_ROUNDS=10

DEFAULT_ADMIN_NAME=Admin
DEFAULT_ADMIN_EMAIL=admin@empresa.com.br
DEFAULT_ADMIN_PASSWORD=troque_esta_senha_no_primeiro_login
DEFAULT_COMPANY_NAME=Empresa
DEFAULT_BASE_NAME=Base Principal
DEFAULT_TIMEZONE=America/Sao_Paulo

APP_BASE_URL=https://estoque.suaempresa.com.br
CORS_ALLOWED_ORIGINS=https://estoque.suaempresa.com.br

PASSWORD_RESET_TOKEN_TTL_MINUTES=60
PASSWORD_RECOVERY_DEBUG=false

SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

VITE_API_URL=/api
VITE_BACKEND_TARGET=http://backend:3000
VITE_ALLOWED_HOSTS=localhost,127.0.0.1
